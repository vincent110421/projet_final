<div class="container">
    <div class="row">
        <div class="col-12 col-md-6 mx-auto">

        {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}

        </div>
    </div>
</div>
