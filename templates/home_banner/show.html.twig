{% extends 'base.html.twig' %}

{% block title %}HomeBanner{% endblock %}

{% block body %}
    <h1>HomeBanner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_banner.id }}</td>
            </tr>
            <tr>
                <th>Banner</th>
                <td>{{ home_banner.banner }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ home_banner.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ home_banner.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_home_banner_index') }}">back to list</a>

    <a href="{{ path('app_home_banner_edit', {'id': home_banner.id}) }}">edit</a>

    {{ include('home_banner/_delete_form.html.twig') }}
{% endblock %}
