<form method="post" action="{{ path('app_home_banner_delete', {'id': home_banner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home_banner.id) }}">
    <button class="btn">Delete</button>
</form>
